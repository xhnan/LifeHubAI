基于以下信息，生成 Service 接口。

## 实体信息
Java 类名: {class_name}
完整包路径: {package_path}

## 要求
请生成简洁的 Service 接口，包含以下内容：

1. **接口定义**：
   - 继承 `IService<{class_name}>`
   - 命名规范：`{class_name}Service`

2. **包声明**：
   - 包路径：`{package_path}.service`

3. **注释**：
   - 添加简洁的 JavaDoc 注释说明 Service 的作用

## 输出格式
请只输出 Java 代码，代码块使用 ```java 包裹。

## 示例格式
```java
package {package_path}.service;

import {package_path}.model.{class_name};
import com.baomidou.mybatisplus.extension.service.IService;

/**
 * {table_comment} 服务接口
 *
 * @author xhn
 * @since {date}
 */
public interface {class_name}Service extends IService<{class_name}> {{

}}
```

请根据上述要求生成代码。
