基于以下信息，生成 MyBatis Plus Mapper 接口。

## 实体信息
Java 类名: {class_name}
完整包路径: {package_path}

## 要求
请生成 MyBatis Plus Mapper 接口，包含以下内容：

1. **接口定义**：
   - 继承 `BaseMapper<{class_name}>`
   - 不使用 `@Mapper` 注解（在配置类中使用 @MapperScan 统一扫描）

2. **包声明**：
   - 包路径：`{package_path}.mapper`

3. **注释**：
   - 添加简洁的 JavaDoc 注释说明 Mapper 的作用

4. **扩展方法**（可选）：
   - 如果有复杂查询需求，可以添加自定义方法
   - 方法名遵循 MyBatis Plus 规范

## 输出格式
请只输出 Java 代码，代码块使用 ```java 包裹。

## 示例格式
```java
package {package_path}.mapper;

import {package_path}.model.{class_name};
import com.baomidou.mybatisplus.core.mapper.BaseMapper;

/**
 * {table_comment} Mapper 接口
 *
 * @author xhn
 * @since {date}
 */
public interface {class_name}Mapper extends BaseMapper<{class_name}> {{

}}
```

请根据上述要求生成代码。
