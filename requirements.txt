# FastAPI
fastapi>=0.115.2,<1.0
starlette>=0.40.0,<1.0
uvicorn[standard]==0.32.0
pydantic==2.9.2

# Database
psycopg2-binary==2.9.9

# AI/LLM
langchain
langchain-openai

# TTS
zhipuai>=2.1.0

# Utilities
python-dotenv==1.0.1
# Required by diffusers on some environments; safe to include for consistent resolution.
importlib-metadata>=6.0

# Imaging (for gradio compatibility)
pillow>=8.0,<12.0

# gRPC
# NOTE: For Windows or Python 3.7, use older version with pre-built wheels:
#   pip install grpcio==1.48.2 grpcio-tools==1.48.2 --only-binary grpcio,grpcio-tools
# Or use platform-specific install scripts:
#   Windows: scripts\install_grpc.bat
#   Linux/Mac: bash scripts/install_grpc.sh
grpcio>=1.48.0,<2.0
grpcio-tools>=1.48.0,<2.0
protobuf>=4.21.0

openai~=2.16.0
requests~=2.32.3